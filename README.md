# Roland Su

A static, minimal GitHub Pages blog.

## Files

- `index.html` is the homepage.
- `posts/index.html` lists all posts.
- `posts/*.html` are individual articles.
- `styles.css` contains the whole visual system.

## Publish on GitHub Pages

In the repository settings, enable GitHub Pages from the `main` branch and the
repository root. No build step is required.
